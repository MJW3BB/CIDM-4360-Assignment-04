using System;

namespace Assignment4
{
    class InvoiceEntry
    {
        static void Main(string[] args)
        {
            int lineNo;
            int Qnty;

            //Returns the line number (lineNo) of the invoice entry
            static void getLineNumber(int lineNo)
            {

            }

            //Change the lineNo of an invoice entry as the passed lineNum param
            static void setLineNumber(int lineNo)
            {

            }

            //Returns the quantity of the item units in the invoice entry
            static void getQnty(int Qnty)
            {

            }
        }
    }
}